export class technologie{
    constructor(public id: number, public name: string, public img: string, public url: string){
        this.id = id;
        this.name = name;
        this.img = img;
        this.url = url;
    }
}